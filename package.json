{
  "name": "prometheus",
  "private": true,
  "type": "module",
  "scripts": {
    "prometheus": "node packages/cli/bin/prometheus.js",
    "cli:build": "pnpm --filter prometheus-notes run build"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "better-sqlite3"
    ]
  }
}
