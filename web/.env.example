# ノートの保存先（デフォルト: ./vault）
# 好きなパスに変更できます。Neovimで直接編集したい場合はローカルのパスを指定。
PROMETHEUS_VAULT_PATH=./vault

# データベースの保存先（デフォルト: ./data/prometheus.db）
PROMETHEUS_DB_PATH=./data/prometheus.db

# サーバーポート（デフォルト: 5173 開発時 / 3000 本番時）
PORT=3000
